import 'package:flutter/material.dart';

class PlaceOrderAllDetailsBottomSheet extends StatelessWidget {
  const PlaceOrderAllDetailsBottomSheet({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Text("hello"),
    );
  }
}
