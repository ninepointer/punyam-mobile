import 'package:flutter/material.dart';

class MandirDetailsPhtosWidget extends StatelessWidget {
  const MandirDetailsPhtosWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text("Photos section here "),
    );
  }
}
